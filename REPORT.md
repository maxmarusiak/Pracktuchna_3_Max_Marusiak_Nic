1. Поясніть різницю між одновимірними та двовимірними масивами. Наведіть приклади використання в роботі.
Одновимірний масив — лінійна структура даних. Використовується для зберігання оцінок студента:
```csharp
public byte[] LabGrades = new byte[10];
2. Чому StringBuilder ефективніший за звичайну конкатенацію? Продемонструйте на прикладі з PortLogger.
Конкатенація створює новий рядок при кожному додаванні. StringBuilder працює з буфером і не створює зайвих об’єктів:
_sb.AppendLine($"{DateTime.Now}: Port {portNumber} = {details}");
3. Як ви реалізували симуляцію портів вводу/виводу? Які масиви використовували?
Створено клас Port з буфером byte[64], статусом відкриття та методами Open(), Close(), Write(), Read(). Буфер — одномірний масив байтів, який моделює реальний порт вводу/виводу.
4. Як інтегрували масиви в already існуючу систему управління студентами?
У Student використовується масив оцінок:
public byte[] LabGrades;
У StudentGroup студента можна прив’язати до порту, записувати дані в порт, читати дані, логувати операції та зберігати/завантажувати JSON.
5. Опишіть логіку роботи з матрицею 16×16.
Матриця портів — двовимірний масив:
Port[,] Matrix = new Port[16, 16];
Кожен елемент — окремий порт. Номер порту = row * 16 + col. ScanMatrix() проходить усі 256 портів і показує їхній стан.
6. Які труднощі виникали при роботі з масивами та як ви їх вирішили?
Труднощі: контроль індексів, перевірка стану порту, робота з 16×16, копіювання масивів, JSON-відновлення. Вирішення: винятки, перевірка IsOpen, цикли, Clone(), DTO для JSON.

7. Порівняйте масиви з колекціями (List<T>). У яких випадках ви свідомо обрали масиви?
List<T> — динамічний. Масив — фіксований і швидкий. Масиви обрані для: оцінок студента (фіксована кількість), буфера порту (64 байти), матриці портів (16×16), JSON (фіксована структура).
8. Що нового ви дізналися під час виконання роботи?
Робота з масивами, симуляція портів, StringBuilder, інтеграція масивів у систему студентів, JSON, винятки, сортування, ICloneable, Git та feature-гілки.

9. Посилання на GitHub репозиторій
https://github.com/maxmarusiak/Pracktuchna_2_Max_Marusiak_Nic
10. Скріншот git log --oneline --graph --all
![git log](image2.png)
11. Кількість створених гілок і комітів
![branches](src/img/image.png)